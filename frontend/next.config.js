/** Next.js App Router config (skeleton) */
/** This config file enables App Router mode for the frontend scaffold. */
module.exports = {
  reactStrictMode: true,
  experimental: {
    appDir: true,
  },
};
